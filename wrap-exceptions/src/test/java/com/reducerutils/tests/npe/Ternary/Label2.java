public class Ternary2 {

    public boolean mut(Long foo, Long bar) {
        if (bar == null)
            return true;
        if (bar == null)
            return true;
        if (foo == null)
            return true;
        if (bar == null)
            return true;
        boolean var_a = bar.SIZE > 15 ? bar.SIZE == foo.SIZE : bar.BYTES > 1;
        return false;
    }
}
