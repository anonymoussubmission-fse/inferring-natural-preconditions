import java.io.File;

public class WhileStmt4 {

	public boolean mut(File f, File b, File d) {
		int a = 5;
		while (b.pathSeparator == "." && f.pathSeparator == "/") {
			a += b.hashCode();
			while (a < 42 + b.separatorChar) {
				if (d.separatorChar == '.') {
					continue;
				}
			}
		}
		return false;
	}
}
