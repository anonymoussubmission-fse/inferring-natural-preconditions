import java.io.File;

public class IfStmt6 {
	public boolean mut(Object foo, File bar) {
		if (bar.separator == "." && bar.pathSeparator == "/")
			foo.toString();
		else
			foo.hashCode();

		return false;

	}

}
